%YAML 1.2
---
#First, set the data folder so we know where to find input and output files
- !SetDataFolder
  datafolder: . #Data folder path relative to the location of this yaml file, unless absolute path provided
#Request files for 2D and 3D simulations
- !RequestFileListRequest
  requestfiles:
    - !DataFile cylinder.2D.yaml
    - !DataFile cylinder.3D.yaml
    # - !DataFile cylinder_sweep.2D.yaml
