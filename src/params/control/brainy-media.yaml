%YAML 1.2
#Parameter input file
#Each document is a separate case run
#Blank (bare) documents are skipped

#Run definition
#meshparams: path (relative to `src`) for mesh parameters definition file
#bcparams: path (relative to `src`) for boundary condition parameters definition file
#master: name of master table for results
---
meshparams: params/mesh/brainy-media.yaml
modelparams: params/model/brainy-media.yaml
master: brainy-media
